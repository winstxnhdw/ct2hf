name: Build

on:
  push:
    branches: main
    paths:
      - .github/workflows/main.yml
      - pyproject.toml
      - poetry.lock
      - ct2hf

jobs:
  test:
    uses: winstxnhdw/actions/.github/workflows/python.yml@main
    with:
      disable-test: true
